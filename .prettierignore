# .html and .js under internal/ are Go templates (html/template with {{ }}, or
# text/template with <<< >>> via component.WithAlpine/WithIIFE). Prettier
# doesn't understand either delimiter and mangles them, so these are excluded
# outright rather than fought with formatting rules.
internal/web/**/*.html
internal/web/**/*.js
